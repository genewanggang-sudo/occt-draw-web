# 工程规范

## 基础原则

本仓库面向三维云�?CAD，工程规范优先保证长期可维护、包边界清晰、类型安全和多人协作稳定�?
文档和产品文案使用中文；代码里的类型名、变量名、函数名和包名保持英文�?

## 工具�?

- Node.js：`24.15.0`
- npm：`11.12.1`
- pnpm：`11.0.9`
- 项目级版本管理：`Volta`
- 页面框架：React
- 构建工具：Vite
- 语言：TypeScript strict
- 格式化：Prettier
- Lint：ESLint flat config
- 依赖方向检查：dependency-cruiser
- 提交门禁：husky + lint-staged + commitlint

## TypeScript 规则

- 禁止使用 `any`�?- 类型覆盖率必须保�?`100%`�?- 优先使用明确领域类型，不用松散对象长期承载业务状态�?- 仅类型依赖必须使�?`type import`�?- workspace 包不使用默认导出�?- 禁止 `export *`，所有公�?API 必须�?`src/index.ts` 显式导出�?- 新写源码统一使用 TypeScript `private`，不新增 `#private`�?

## 导入规则

- 跨包导入只能走包入口，例�?`@occt-draw/math`�?- 禁止 deep import，例�?`@occt-draw/core/src/foo`�?- 禁止相对路径跨包导入，例�?`../../packages/foo/src`�?- 包内文件可以使用相对导入，但不能形成循环依赖�?

## 包边�?

## 依赖方向

````txt
math -> none
protocol -> none
core -> math
sketch -> core, math
constraints -> sketch, core, math
parametrics -> core
cad-rendering -> webgl-engine, core, sketch, math
webgl-engine -> math
wasm-bridge -> protocol
worker-client -> protocol
worker-runtime -> wasm-bridge, protocol
cloud-client -> protocol
apps/web -> editor, ui, webgl-engine, worker-client, cloud-client 等运行时�?```

## Wasm �?TypeScript 分工

TypeScript 负责编辑器状态和交互逻辑�?
- 命令系统
- 选择系统
- 草图工具�?- 约束数据模型
- 参数表达�?- 文档编辑事务
- 显示模型投影

Wasm 负责几何内核能力�?
- 高精度几何计�?- 拓扑生成
- 布尔运算
- 曲线曲面计算
- 模型网格化或几何输出

## 校验命令

根命令是 CI 和本地校验的统一入口�?
```bash
pnpm check
````

其中包含�?

- `pnpm lint`
- `pnpm typecheck`
- `pnpm type-coverage`
- `pnpm format:check`
- `pnpm depcruise`
- `pnpm build`

## 数据模型层依赖规则（2026-05-17�?

当前数据模型层的强制依赖方向�?

```txt
cad-model -> sketch, core, math
sketch    -> core, math
core      -> math
math      -> none
```

规则说明�?

- `core` 是通用数据机制层，不放 CAD 文档业务对象�?- `sketch` 是草图领域包，不依赖 `cad-model`，也不查�?Feature tree�?- `cad-model` �?CAD 文档模型包，负责�?Sketch 作为内置 Feature 挂到 `PartStudio` �?feature/payload 体系里�?- `dependency-cruiser` 是该依赖方向的自动校验入口�?
