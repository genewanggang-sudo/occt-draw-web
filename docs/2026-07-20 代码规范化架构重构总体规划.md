# 代码规范化架构重构总体规划

更新日期：2026-07-20

## 1. 定位

本文件是 `occt-draw-web` 当前唯一有效的工程规划文档。它定义一次以行为保持为前提的大规模代码规范化重构：将已有实现整理为职责单一、命名稳定、易于定位的架构，同时不改变产品功能、渲染结果、交互时序、数据格式和 package 边界。

本次工作不是功能开发，不借机修复交互问题、优化性能、替换渲染算法或改变 UI。发现的问题应记录在批次审查中，另行冻结范围后处理。

## 2. 已确认的产品基线

### 2.1 工作台与文档

- React 工作台包含特征树、属性面板、命令工具栏、草图编辑面板，以及打开、保存、撤销和重做。
- 默认 CAD 文档包含 Part Studio、原点和 XY/YZ/ZX 基准面。
- 文档通过 JSON 编解码打开和保存；序列化格式、字段、ID 和版本语义属于兼容契约。

### 2.2 草图编辑

- 可以选择基准面进入草图，确认时合并编辑 scope，取消时回滚；也可以从特征树再次进入草图编辑。
- 已接通临时 Draft 预览和正式 document request 提交的工具包括：点、直线、中点线、样条、拐角矩形、中心矩形、对齐矩形、圆、三点圆、内切/外接正多边形、三点圆弧、相切圆弧、圆心圆弧、椭圆、椭圆弧和圆锥曲线。
- 支持对象与草图实体拾取、悬停预选、活动草图顶点拖拽、删除草图实体、撤销和重做。
- `extrude` 目前只有命令定义和 UI 入口：`hasSketchProfile` 仍恒为 `false`，没有可执行的 ExtrudeCommand。约束、尺寸和 profile 也是数据占位，不应在本次重构中扩展。

### 2.3 渲染与视口

- CAD 文档、草图和 Draft 会先投影为 `CanvasScene`，再转换为 `RenderGraph`。
- WebGL2 当前生产渲染顺序固定为 `ColorPass -> HighlightPass -> OverlayPass`。`WebGLRenderer` 负责场景缓存路径，`WebGLImmediateRenderer` 仍用于高亮与叠加即时绘制，两者不可合并或互换。
- 支持基准面、标签、活动草图平面、草图边/曲线/顶点/自由点、临时预览、选择与预选高亮。
- 支持右键旋转、中键平移、滚轮缩放、适配视图、标准视图、ViewCube 面/角/箭头操作和导航深度采样。

## 3. 不可变约束

1. 不新增、删除、合并或拆分 workspace package。任何 package 层级调整都必须先获得用户明确确认。
2. 不修改 `AGENTS.md`。
3. 不新增测试文件，也不为本次规范化重构改写现有测试文件。
4. 不改变根 `package.json` 的用户已有改动，也不夹带依赖升级、构建配置调整或格式化全仓库。
5. `apps/web` 只能依赖 `@occt-draw/editor` 与 `@occt-draw/ui`；其他依赖白名单继续由 `dependency-cruiser.cjs` 约束。
6. package 根 `src/index.ts` 是唯一公开入口。已有导出符号、构造参数、返回值、事件顺序和持久化格式必须保持兼容。
7. 规范化阶段不改变 render pass 顺序、shader source、uniform/attribute 协议、GPU 状态、对象 ID、primitive metadata、dirty flag/cache key 语义，或 `EditorViewport.sync()` 的图重建与渲染时机。
8. 任何 Onshape 或其他外部资料的下载、截图、脚本和分析产物只放入 Git 忽略的 `tmp/`；每个分析目录应有可检索的 `INDEX.md`，不得再次写入 `docs/` 或提交。

## 4. 当前分层与关键调用链

```txt
apps/web -> editor, ui
editor -> cad-canvas, cad-model, canvas, platform, sketch, snapping, core, math
cad-canvas -> cad-model, sketch, core, canvas, math
platform -> canvas, snapping, core, math
canvas -> webgl-engine, math
cad-model -> sketch, core, math
sketch / snapping -> core, math
core / webgl-engine -> math
math / ui -> no workspace dependency
```

编辑和渲染主链必须保持不变：

```txt
React App
  -> Application.initCanvas()
  -> EditorViewport
  -> RenderEngine

EditorState sync
  -> createEditorRenderGraph()
  -> CadCanvasAdapter.createScene()
  -> CanvasScene
  -> CanvasRenderGraphProjector
  -> RenderGraph
  -> ColorPass -> HighlightPass -> OverlayPass
```

输入、选择和相机主链必须保持不变：

```txt
DOM pointer / wheel / key
  -> ViewportInput
  -> ViewportInteractor
  -> ViewCubeController
  -> CommandManager
  -> EditorDefaultController
  -> CameraNavigationController

selection pick
  -> EditorViewport.pickSelectionTarget()
  -> PickService
  -> CanvasPickService
  -> RenderObjectPicker
```

当前 `legacy/*` 是旧 RenderScene/RenderNode 与新 RenderGraph/RenderObject 的内部兼容适配，不是当前应用渲染主路径。规范化过程中只隔离、审计和保留其兼容性；是否删除必须作为最后一个单独批次确认。

## 5. 一文件一类规范

“一文件一类”在本项目中的可执行定义如下：

1. 每个可实例化的领域类、控制器、命令、渲染对象、pass、adapter、store 或 React 组件只有一个主文件，文件名与主导出名一致，例如 `Sketch.ts`、`AddCircleRequest.ts`、`ViewCubeController.ts`。
2. 一个实现文件最多承载一个同级主运行时抽象。描述该抽象的输入、输出、私有 helper type 和常量可以就近保留；跨文件或跨 package 的稳定契约独立为主题明确的 `*Types.ts` 文件。
3. 纯函数模块不是为了形式强行包装为 class。数学公式、不可实例化的 factory、映射器和常量表可以保留函数形式，但一个文件只能表达一个清楚命名的概念，不能成为无边界的 `utils` 集合。
4. `index.ts` 只负责公开 re-export，不承载运行时业务、类实现或跨层编排。
5. 每次拆分后，调用方仍通过原 package 根入口或原有内部门面导入。允许新增内部目录和兼容 re-export，禁止跨 package 深导入来绕过边界。
6. 大类可拆出协作对象，但保留原类作为门面，直到所有调用点完成稳定迁移；不将纯重构和 API 重设计放在同一批次。
7. 类的依赖必须通过构造参数或明确的上下文对象注入；不引入隐藏单例、循环 import 或运行时动态装配来规避拆分难度。

## 6. 优先规范化热点

| 优先级 | 当前位置                                                                                                                                | 当前问题                                                                | 目标形态                                                                                      |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 高     | `packages/cad-model/src/sketchRequests.ts`                                                                                              | 集中 16 个 request 类                                                   | 每个 request 独立文件，共享目标解析和 request 基类独立                                        |
| 高     | `packages/sketch/src/model/sketch.ts`                                                                                                   | 集中 Sketch、Plane、实体集、store、State 等 13 个类                     | Plane、Sketch、各 entity set、各 store、State 分文件，保留聚合门面                            |
| 高     | `packages/sketch/src/geometry/geometry.ts`                                                                                              | 集中 Point/Line/Circle/Ellipse/Arc/Conic/Spline 等 8 个类               | 每个图元类、snapshot 和恢复工厂按概念归位                                                     |
| 高     | `packages/editor/src/application/EditorViewport.ts`                                                                                     | 混合 DOM 生命周期、尺寸监听、图构建、渲染、动画、拾取和 ViewCube 控制器 | `EditorViewport` 保持外观，协作对象独立为 renderer sync、尺寸观察、动画和 ViewCube controller |
| 高     | `packages/webgl-engine/src/viewCube.ts`                                                                                                 | 混合布局、几何、标签、渲染输入与命中测试                                | 按布局、几何、标签、命中测试和 overlay command 拆分，保持视觉与点击结果                       |
| 高     | `packages/webgl-engine/src/webgl/webglRenderer.ts`                                                                                      | 混合资源生命周期、帧状态、绑定、绘制和标签/marker 处理                  | 明确分离帧生命周期、资源绑定和各 primitive 绘制执行器                                         |
| 中     | `packages/sketch/src/model/SketchPrimitiveBuilder.ts`                                                                                   | 混合图元创建、拓扑复用、ID 分配、删除和变更记录                         | 保留 builder 门面，按图元构造和变更协作对象拆分                                               |
| 中     | `packages/core/src/editing/changeSet.ts`、`packages/cad-model/src/document.ts`、`packages/webgl-engine/src/geometry/geometryBuffer.ts`  | 多个同级类共存                                                          | 每个同级运行时类独立文件，稳定 re-export                                                      |
| 中     | `packages/editor/src/application/ViewportControllers.ts`、`apps/web/src/app/App.tsx`、`apps/web/src/editor/commands/CommandToolbar.tsx` | 多种协调职责或局部 UI 组成混杂                                          | 以 controller、hook、view section 或工具组为边界拆分，保持 React 事件语义                     |
| 中     | 各草图命令                                                                                                                              | 已是一命令一文件，但状态机、投影、预览和 request 装配存在重复           | 抽取命令内部协作对象；命令类继续保留在原文件且不改变状态机步骤                                |

## 7. 分批实施路线

每个批次只做一个可独立运行、可独立验收、可独立提交的最小改动。下表是推荐顺序；进入每个批次前仍须重新冻结精确文件清单和验收场景。

| 批次 | 范围                                           | 交付边界                                                                                        |
| ---- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 0    | 文档重置                                       | 清空旧 `docs`，新增本规划，修复唯一文档入口                                                     |
| 1    | `@occt-draw/math`                              | 拆分少量同文件数学类，保持值对象、数值结果与根导出不变                                          |
| 2    | `@occt-draw/core`                              | 拆分编辑事务、change set、history 等同级运行时类，保持 DocumentSession/Transaction 语义         |
| 3    | `@occt-draw/sketch` 几何                       | 拆分图元类、snapshot 和采样协作对象，保持图元计算、ID 和序列化                                  |
| 4    | `@occt-draw/sketch` 模型                       | 拆分 Sketch、entity set、store、state 和 primitive builder 协作对象，保持编辑事务与 Draft 行为  |
| 5    | `@occt-draw/cad-model`                         | 拆分 document/object/request 类，尤其是草图 request 集合，保持 JSON 和 feature/payload 行为     |
| 6    | `@occt-draw/webgl-engine` 数据层               | 拆分 geometry、style、scene、buffer 和 render state 同级类，保持 RenderGraph DTO 与根导出       |
| 7    | `@occt-draw/webgl-engine` 运行时               | 拆分 renderer、pipeline、navigation depth、ViewCube 协作对象；固定 pass、shader 和 GPU 时序     |
| 8    | `@occt-draw/canvas` 与 `@occt-draw/cad-canvas` | 保持 `CanvasScene -> RenderGraph`、metadata、pick 与 CAD/Sketch adapter 的边界                  |
| 9    | `@occt-draw/snapping` 与 `@occt-draw/platform` | 规范吸附、输入事件和导航协作对象，保持 pointer capture 与事件短路顺序                           |
| 10   | `@occt-draw/editor` 运行时                     | 拆分 Application、EditorViewport、EditorController 的组装职责，保持状态同步、动画与会话生命周期 |
| 11   | `@occt-draw/editor` 命令与 view model          | 收敛命令的预览/投影/请求协作对象，保持每个工具的点击步骤和 Draft 预览                           |
| 12   | `apps/web` 与 `@occt-draw/ui`                  | 拆分 React 组合、文件操作和工具栏呈现，保持布局、按钮、快捷操作和状态显示                       |
| 13   | legacy 审计                                    | 只在无消费者和公开兼容确认后，单独决定保留、隔离或删除 `legacy/*`                               |

## 8. 每批执行协议

1. 冻结范围：列出要移动、创建、删除和保留门面的每个文件，写明不触碰的邻近模块。
2. 冻结契约：记录 package 根导出、运行时调用顺序、序列化字段、对象 ID 和关键输入/输出。
3. 记录视觉基线：启动现有 Web 应用，将截图、操作记录和外部资料放在 `tmp/` 对应批次目录，并建立 `INDEX.md`。
4. 只做结构提取、移动和 re-export；发现行为修复、性能优化、样式调整或包边界需求时停止并单独规划。
5. 执行静态检查：`pnpm lint`、`pnpm -r typecheck`、`pnpm depcruise` 和 `pnpm --filter @occt-draw/web build`。不新增或修改测试文件。
6. 以浏览器视觉和交互验收为准，完成本批相关路径后进行代码审查；所有发现的问题必须在本批修复后才可提交。
7. 暂存本批文件，确认 `git diff --cached` 不含用户无关改动，再提交一个语义明确的 commit。

## 9. 浏览器验收清单

所有涉及运行时代码的批次，至少覆盖受影响区域及其上游关键路径：

- 启动后显示 `WebGL2 ready`，原点与三基准面正常显示，初始帧没有黑屏或异常闪烁。
- 悬停预选、对象/草图实体选择、高亮、顶点拖拽和删除的对象目标与外观保持一致。
- 选择基准面进入草图，分别确认和取消；再次从特征树编辑草图。
- 对本批影响的草图工具完成多阶段预览与提交，检查 preview、正式图元、吸附、对象 ID、primitive metadata 和撤销/重做。
- 右键旋转、中键平移、滚轮缩放、适配视图、标准视图和 ViewCube 面/角/箭头均保持正常。
- 打开与保存文档后可恢复既有文档几何、feature 和 payload，并保持视口可用。
- 涉及渲染时，确认 pass 顺序、叠加层、标签、虚线、点字形、选中/预选和草图颜色没有视觉回归。

## 10. 完成标准

本次规范化重构完成时，应同时满足：

- 每个运行时类和 React 组件都有唯一、可预测、以名称命名的主文件。
- 不再存在承载多个同级业务类的大型聚合文件；纯函数文件也有单一概念边界。
- 所有 package 的根导出、依赖方向、document 文件格式、交互顺序和渲染输出保持兼容。
- 每个批次都有冻结范围、浏览器验收记录、审查结果和独立 commit。
- 不新增测试文件，不新增 package，不将外部分析产物提交到仓库。
