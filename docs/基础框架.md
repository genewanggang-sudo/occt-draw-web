# 基础框架

## 文档目的

本文记录当前仓库的基础框架状态，作为后续实现、评审和协作时的共同参考�?
当前项目不再以二维出图编辑器为目标，而是面向三维云端 CAD 工作台�?

## 当前基础设施

- `apps/web`：React + Vite 浏览器工作台�?- `packages/*`：按领域和基础能力拆分的内部包�?- `docs`：中文工程文档�?- `package.json` �?`volta` 字段：当前项目专�?Node、npm �?pnpm 版本管理�?
  当前项目运行时版本：

- Node.js：`24.15.0`
- npm：`11.12.1`
- pnpm：`11.0.9`

这些版本只对当前项目生效，不改变其它项目的全局默认版本�?

## 当前包结�?

基础包：

- `math`
- `protocol`
- `config`

领域包：

- `core`
- `editor`
- `sketch`
- `constraints`
- `parametrics`
- `display`

运行时包�?

- `renderer`
- `webgl-engine`
- `worker-client`
- `worker-runtime`
- `wasm-bridge`
- `cloud-client`

应用�?UI�?

- `apps/web`
- `ui`

## 工程护栏

当前已经启用�?

- TypeScript strict
- ESLint flat config
- Prettier
- EditorConfig
- `type-coverage` 100%
- dependency-cruiser 依赖方向检�?- husky + lint-staged
- commitlint
- GitHub Actions CI
- Dependabot

根命令：

- `pnpm lint`
- `pnpm typecheck`
- `pnpm type-coverage`
- `pnpm format:check`
- `pnpm depcruise`
- `pnpm build`
- `pnpm check`

这段不应该从基础框架文档中删除。`工程规范.md` 会展开规则细节，但 `基础框架.md` 需要保留工程护栏快照，方便快速了解当前基础设施成熟度�?
