# Agent Instructions

- In this monorepo, when changing package structure, use allowlisted file-level moves only; never move an entire package directory that may contain `node_modules`, `dist`, or workspace links.
