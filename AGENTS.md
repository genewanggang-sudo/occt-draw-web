# Agent 指令

- 在这个 monorepo 中修改包结构时，只能按白名单进行文件级迁移；不要整体移动可能包含 `node_modules`、`dist` 或 workspace link 的包目录。

- 如果项目改动会新增、删除或改变包之间的依赖关系，必须先向用户说明依赖变化、影响范围和原因，并等待用户手动确认；不要自动执行这类依赖关系变更。

- 制定开发任务计划时，必须先基于当前包职责和依赖关系说明需要在哪些包中新增、删除或修改什么内容；如果发现现有包职责、依赖关系或实现存在前置问题，必须把这些前置修正单独列出并等待用户确认，只有确认前置依赖和边界问题已解决后，才能继续规划或实施新功能开发。

- 任何涉及两个及以上 package/app 的改动，在编码前必须先暂停并输出“包职责与改动边界确认”，逐项列出每个 package/app 需要新增、修改、删除的文件或 API、原因、影响范围，以及是否改变依赖关系；必须等待用户明确确认后才能开始编码。即使不改变依赖关系，也必须执行该确认步骤。

- 未经用户确认，不允许新增、删除或修改跨包 public API、导出项、Request/Edit 类型、共享 helper、workspace package 边界，或会被其他 package/app 直接 import 的模块。仅限单包内部且不新增 public export 的实现细节修改可以直接执行。
