1.此项目最终想要达到的效果是一个和onshape无限接近的CAD产品，尤其是交互和渲染效果，需要深入分析onshape的实现，对于Shader和Pass等能复用的直接复用
分析onshape可以使用一个固定链接

2.分析onshape或者其它资料时产生的所有文件，应该放在项目的tmp目录下，且tmp目录不允许提交，分析的产物需要汇总做好索引，确保能用、好用

3.开发实施阶段不需要写任何测试文件，因为会大幅拖慢开发进度，直接使用浏览器开启视觉效果验收

4.对于一个功能比较大的任务，需要拆分好实施方案，分批次执行，每批次只产出一个有完整功能的最小化改动，然后提交

5.对于每批次的任务，开始前需要先冻结实施范围，给出详细的实施和验收计划，计划生成后，你自己审查一遍，如果有问题修改，然后进入实施

6.实施过程中可以交给子智能体进行的分析、查看、修改等任务都需要拆出去，并行开发提高开发速度，但主对话要做好总体把控

7.每批次任务实施结束后，需要自己对当前批次改动执行一次审查，审查结束后，将需要修改的问题全部修复，然后验收确保没问题了，再提交

8.整个项目的分层，如果再开发过程中有需要调整的地方需要和我确认才能增删包，其它情况一律不允许改动

9.任何新功能、重构或拆分任务开始前，必须先完成整体抽象与封装设计，并在实施计划中明确：领域概念、职责边界、类清单、稳定接口、类间依赖方向、数据与生命周期边界。凡是能够形成明确职责或概念的东西，都必须先抽象并封装为命名清晰的类；包括领域对象、值对象、服务、策略、工厂、构建器、验证器、映射器、计算器、状态机、控制器、适配器、命令和协调器。算法、状态转换、映射、校验、构造、事件处理和 helper 不得作为散落的模块级自由函数存在，必须成为所属类的实例方法或静态方法；一个类只承担一个明确职责，尽量一个文件对应一个类。仅类型、接口、常量和确实无法合理建模为类的语言级定义可以不使用类，且必须在设计阶段说明原因。禁止在编码过程中临时堆放 helper 或事后补抽象；发现职责不清、依赖倒置、生命周期不明或 helper 归属不明时，必须先修正设计再编码。

10.该文件没有用户明确同意不允许修改
