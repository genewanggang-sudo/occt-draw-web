1.此项目最终目标是实现一个尽可能接近 Onshape 的 CAD 产品。需要深入分析 Onshape 的实现；对于可复用的 Shader、Pass 等实现，直接复用。

分析 Onshape 使用固定链接：
https://cad.onshape.com/documents/cb24420a26a5ed84801750a9/w/6afad649721589e7fdfade2a/e/86ba8a42171d7bde0922e59d

若访问该链接被重定向到登录页，停止当前调研步骤并通知用户登录。

2.分析 Onshape 或AutoDesk的资料或其他资料时产生的所有资料及其索引，均放入项目 `tmp/` 目录；`tmp/` 中的内容不得提交。分析产物必须汇总并建立索引，确保后续可查、可用。

3.分析 Onshape或者AutoDesk的资料或者其它等资料时，可以使用内置浏览器或者自己开发的chrome skill来进行，使用chrome skill过程中，如果发现有实现或者使用起来需要优化的地方，需要先将它优化好，然后再继续任务。

4.开发实施阶段不需要写任何测试文件，因为会大幅拖慢开发进度，直接使用浏览器开启视觉效果验收

5.对于较大的功能任务，必须先拆分实施方案并分批执行；每个批次只产出一个最小且功能完整的改动。

6.对于每批次的任务，开始前需要先冻结实施范围，给出详细的实施和验收计划，计划生成后，你自己审查一遍，如果有问题修改，然后进入实施

7.实施过程中可以交给子智能体进行的分析、查看、修改等任务都需要拆出去，不同子智能体不得同时修改同一文件，主智能体负责最终集成

8.每批次任务实施结束后，必须自行审查当前批次改动，修复发现的全部问题并完成验收。随后说明本批次的改动内容和验收结果，停下等待用户审查；未经用户明确指令，不得继续后续批次或擅自提交。

9.未经用户明确同意，不得新增、删除、合并或拆分 package，不得将模块移动到其他 package，也不得改变 package 职责或 package 间依赖关系。

10.代码必须保持可读、优雅和职责清晰。业务、渲染、交互、命令、状态和资源管理相关的行为，必须归属到职责明确的类；禁止新增没有明确领域归属的自由函数、跨模块散落的方法，以及 utils、helpers、common 等收容文件。

原则上一个文件只定义一个主类，文件名与类名保持一致。新增可复用能力时，优先放入职责正确的已有类；确有必要时，创建职责和命名明确的新类。与类相关的所有辅助逻辑必须作为该类的 private、protected 或 private static 方法实现，不得在类文件顶层定义独立辅助函数。

React 组件、Hooks 和数学包中职责明确的纯计算 API 可以作为例外；但它们必须有明确领域归属，不能成为收纳杂项逻辑的入口。

11.未经用户明确同意，严禁修改 AGENTS.md 或 `docs/` 下的任何文档；确有修改需求时，必须停止并先征得用户同意。
