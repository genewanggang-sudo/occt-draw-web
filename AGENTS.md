# Agent 指令

- 在这个 monorepo 中修改包结构时，只能按白名单进行文件级迁移；不要整体移动可能包含 `node_modules`、`dist` 或 workspace link 的包目录。

- 如果项目改动会新增、删除或改变包之间的依赖关系，必须先向用户说明依赖变化、影响范围和原因，并等待用户手动确认；不要自动执行这类依赖关系变更。

- 制定开发任务计划时，必须先基于当前包职责和依赖关系说明需要在哪些包中新增、删除或修改什么内容；如果发现现有包职责、依赖关系或实现存在前置问题，必须把这些前置修正单独列出并等待用户确认，只有确认前置依赖和边界问题已解决后，才能继续规划或实施新功能开发。
