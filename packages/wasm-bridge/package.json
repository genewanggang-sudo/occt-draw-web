{
    "name": "@occt-draw/wasm-bridge",
    "private": true,
    "version": "0.1.0",
    "types": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "pnpm exec tsc -p tsconfig.build.json --pretty false",
        "clean": "rimraf dist tsconfig.build.tsbuildinfo tsconfig.tsbuildinfo",
        "lint": "pnpm exec eslint src --ext .ts",
        "typecheck": "pnpm exec tsc -p tsconfig.json --pretty false"
    }
}
